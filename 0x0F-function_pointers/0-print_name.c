#include "function_pointers.h"
/**
 * print_name - should print a name
 * @name: name to be printed
 * @f: function to pick
 * Return: void
*/
void print_name(char *name, void (*f)(char *c))
{
	if (name && f);
	f(name);
}
